const recuperar = document.querySelector("#recuperar"), feedbackRecuperar = recuperar.querySelector(".feedback");
recuperar.addEventListener("submit", async event => { event.preventDefault(); feedbackRecuperar.textContent = ""; try { const response = await fetch("/Auth/RecuperarContrasena", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ correo: recuperar.correo.value }) }); if (!response.ok) throw new Error(await response.text()); feedbackRecuperar.textContent = "Si existe una cuenta asociada, recibirás un correo con los pasos a seguir."; } catch (error) { feedbackRecuperar.textContent = error.message; } });
